import Link from "next/link";
import { Activity, ArrowUpRight, CalendarClock, IndianRupee, Inbox, TrendingUp } from "lucide-react";
import { site } from "@/content/site";
import { getDashboardMetrics } from "@/lib/admin/data";
import { MetricCard } from "@/components/admin/MetricCard";
import { TrendChart } from "@/components/admin/TrendChart";

export const dynamic = "force-dynamic";

export default async function AdminDashboardPage() {
  const metrics = await getDashboardMetrics();
  const funnelMax = Math.max(...metrics.funnel.map((item) => item.value), 1);
  const carMax = Math.max(...metrics.topCars.map((item) => item.value), 1);
  const sourceMax = Math.max(...metrics.sources.map((item) => item.value), 1);
  return <><div className="admin-page-head"><div><span className="admin-overline">Live business pulse</span><h1>Good decisions start here.</h1><p>Demand, conversion and revenue at a glance.</p></div><div className="admin-head-actions"><Link className="admin-secondary-button" href="/admin/enquiries">Open inbox <ArrowUpRight size={14} /></Link></div></div><section className="admin-metrics"><MetricCard label="Enquiries this week" value={String(metrics.thisWeek)} detail={<span className={metrics.weekChange >= 0 ? "admin-positive" : "admin-negative"}>{metrics.weekChange >= 0 ? "+" : ""}{metrics.weekChange}% vs last week</span>} icon={Inbox} color="#dce9ff" ink="#3a6ba7" /><MetricCard label="Conversion rate" value={`${metrics.conversionRate}%`} detail="Completed bookings / enquiries" icon={TrendingUp} color="#e0f1e8" /><MetricCard label="Revenue this month" value={`₹${metrics.revenueThisMonth.toLocaleString("en-IN")}`} detail="From completed bookings" icon={IndianRupee} color="#ffeadc" ink="#b95735" /><MetricCard label="Active bookings" value={String(metrics.activeBookings)} detail="Confirmed or on the road" icon={CalendarClock} color="#eee5ff" ink="#734ca1" /></section><section className="admin-dashboard-grid"><article className="admin-card"><div className="admin-card-head"><div><h2>Enquiry trend</h2><span className="admin-card-subtitle">Weekly demand over the last 8 weeks</span></div><Activity size={18} color="var(--teal)" /></div><TrendChart data={metrics.trend} /></article><article className="admin-card"><div className="admin-card-head"><div><h2>Conversion funnel</h2><span className="admin-card-subtitle">Where potential customers drop off</span></div></div><div className="admin-funnel">{metrics.funnel.map((item, index) => { const previous = index ? metrics.funnel[index - 1].value : item.value; const drop = previous ? Math.max(0, Math.round((1 - item.value / previous) * 100)) : 0; return <div className="admin-funnel-row" key={item.label}><span>{item.label}{index > 0 ? <small style={{display:"block",color:"var(--muted)",marginTop:2}}>{drop}% drop</small> : null}</span><div className="admin-funnel-track"><div className="admin-funnel-fill" style={{width:`${Math.max(2,(item.value / funnelMax) * 100)}%`}} /></div><strong className="admin-funnel-value">{item.value}</strong></div>; })}</div></article><article className="admin-card"><div className="admin-card-head"><div><h2>Top requested cars</h2><span className="admin-card-subtitle">What customers want most</span></div></div><div className="admin-ranking">{metrics.topCars.length ? metrics.topCars.map((item) => <div className="admin-rank-row" key={item.slug}><span>{site.fleet.find((car) => car.slug === item.slug)?.name || item.slug}</span><div className="admin-rank-track"><div className="admin-rank-fill" style={{width:`${(item.value/carMax)*100}%`}} /></div><strong>{item.value}</strong></div>) : <div className="admin-empty">No car requests yet.</div>}</div></article><article className="admin-card"><div className="admin-card-head"><div><h2>Traffic sources</h2><span className="admin-card-subtitle">Where enquiries originate</span></div></div><div className="admin-ranking">{metrics.sources.length ? metrics.sources.map((item) => <div className="admin-rank-row" key={item.label}><span style={{textTransform:"capitalize"}}>{item.label}</span><div className="admin-rank-track"><div className="admin-rank-fill" style={{width:`${(item.value/sourceMax)*100}%`,background:"linear-gradient(90deg,#ed684d,#ffc162)"}} /></div><strong>{item.value}</strong></div>) : <div className="admin-empty">Source data will appear with enquiries.</div>}</div></article></section></>;
}
