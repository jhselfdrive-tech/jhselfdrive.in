export function TrendChart({ data }: { data: { label: string; value: number }[] }) {
  if (!data.length) return <div className="admin-empty">Trend data will appear after enquiries arrive.</div>;
  const max = Math.max(...data.map((item) => item.value), 1);
  const width = 640; const height = 220; const gap = 14; const barWidth = (width - 40 - gap * (data.length - 1)) / data.length;
  return <svg className="admin-chart" viewBox={`0 0 ${width} ${height}`} role="img" aria-label="Weekly enquiry trend"><defs><linearGradient id="barGradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#ed684d" /><stop offset="1" stopColor="#ffb064" /></linearGradient></defs>{[0,1,2,3].map((line) => <line className="admin-chart-grid" x1="30" x2={width} y1={30 + line * 45} y2={30 + line * 45} key={line} />)}{data.map((item, index) => { const barHeight = (item.value / max) * 145; const x = 34 + index * (barWidth + gap); const y = 178 - barHeight; return <g key={item.label}><rect className="admin-chart-bar" x={x} y={y} width={Math.max(barWidth, 8)} height={barHeight} rx="6" /><text className="admin-chart-label" x={x + barWidth / 2} y="198" textAnchor="middle">{new Date(item.label).toLocaleDateString("en-IN", { day: "numeric", month: "short" })}</text><text className="admin-chart-label" x={x + barWidth / 2} y={y - 7} textAnchor="middle">{item.value}</text></g>; })}</svg>;
}
